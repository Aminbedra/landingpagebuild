# Dependencies
node_modules/

# Wrangler
.wrangler/
.dev.vars
.dev.vars.*

# Env / secrets
.env
.env.*
# ...except the committed template itself, which has no real values in it.
!.env.example

# OS
.DS_Store

# Backups made during local work
worker/src.backup-*

# Build output
dist/
