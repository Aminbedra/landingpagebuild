# Public, build-time config — inlined into the client bundle by Vite.
# Nothing secret goes here. Mirrors ../astro/.env.example.
#
# .env itself is gitignored — copy this file to .env and fill it in.

# Staging: https://api.staging.landingpagebuild.com
# Production: not wired yet — see STATUS.md's Phase 4 entry.
PUBLIC_WORKER_API_URL=https://api.staging.landingpagebuild.com
